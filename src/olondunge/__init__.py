"""Olondunge: one mind that allocates work across the agent CLIs on your machine.

Importing this package must stay free of third party imports. The tri activation hooks
import `olondunge.tristack` straight from a plugin checkout with nothing installed, so
anything heavier than the standard library belongs in `olondunge.server`.
"""

__version__ = "0.1.0"
